<%= form_for(@blog_source) do |f| %>
  <% if @blog_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_source.errors.count, "error") %> prohibited this blog_source from being saved:</h2>

      <ul>
      <% @blog_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
