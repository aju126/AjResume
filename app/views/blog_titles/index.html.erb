<p id="notice"><%= notice %></p>

<h1>Listing Blog Titles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blog_titles.each do |blog_title| %>
      <tr>
        <td><%= link_to 'Show', blog_title %></td>
        <td><%= link_to 'Edit', edit_blog_title_path(blog_title) %></td>
        <td><%= link_to 'Destroy', blog_title, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog title', new_blog_title_path %>
